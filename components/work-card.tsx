"use client";

import {
    Card,
    CardContent,
    CardDescription,
    CardFooter,
    CardHeader,
    CardTitle,
} from "@/components/ui/card";

const WorkCard = () => {
    return (
        <Card>
            <CardHeader>
                <CardTitle>My Work</CardTitle>
            </CardHeader>
        </Card>
    );
};

export default WorkCard;
