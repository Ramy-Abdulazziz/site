"use client";

import {
    Card,
    CardContent,
    CardDescription,
    CardFooter,
    CardHeader,
    CardTitle,
} from "@/components/ui/card";

const ContactCard = () => {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Contact Me</CardTitle>
            </CardHeader>
        </Card>
    );
};

export default ContactCard;
