"use client";

import {
    Card,
    CardContent,
    CardDescription,
    CardFooter,
    CardHeader,
    CardTitle,
} from "@/components/ui/card";

const SocialCard = () => {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Socials</CardTitle>
            </CardHeader>
        </Card>
    );
};

export default SocialCard;
